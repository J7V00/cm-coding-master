name: Publish macOS App

on:
  push:
    branches:
      - main
    paths-ignore:
      - "downloads/Coding-Master-macOS.dmg"

permissions:
  contents: write

jobs:
  publish-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build macOS DMG
        uses: tauri-apps/tauri-action@v0
        with:
          args: --bundles dmg

      - name: Replace macOS download file
        shell: bash
        run: |
          set -e
          DMG="$(find src-tauri/target/release/bundle/dmg -maxdepth 1 -type f -name '*.dmg' | head -n 1)"
          if [ -z "$DMG" ]; then
            echo "macOS DMG was not found."
            exit 1
          fi
          cp "$DMG" "downloads/Coding-Master-macOS.dmg"

      - name: Commit macOS installer
        shell: bash
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add downloads/Coding-Master-macOS.dmg
          git commit -m "Publish macOS App [skip ci]" || echo "No changes to commit"
          git push
